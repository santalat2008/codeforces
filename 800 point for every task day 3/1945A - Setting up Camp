import sys
input = sys.stdin.read
data = input().split()

t = int(data[0])
idx = 1

for _ in range(t):
    a = int(data[idx])
    b = int(data[idx+1])
    c = int(data[idx+2])
    idx += 3

    if (b + 2) // 3 * 3 > b + c:
        print(-1)
    else:
        print(a + (b + c + 2) // 3)
