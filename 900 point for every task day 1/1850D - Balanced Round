for _ in range(int(input())):
    n, k = map(int, input().split())
    a = sorted(map(int, input().split()))
    m = c = 1
    for i in range(1, n):
        c = c + 1 if a[i]-a[i-1] <= k else 1
        m = max(m, c)
    print(n - m)
